# src/ — core library package
#
# Exposes the six reusable modules so callers can write:
#   from src.tcav_scoring import evaluate_tcav
#   from src.similarity_scoring import SuperpixelGraphSimilarityScorer
#   etc.
